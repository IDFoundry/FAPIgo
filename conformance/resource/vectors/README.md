# resource vectors

Test vectors for resource-server access-token and DPoP verification: valid
and invalid proofs, replay attempts, mismatched method/URI binding, and
`cnf.jkt` mismatches. The OIDF suite does not cover the resource-server
role, so these vectors are maintained in this module instead.
